# Dependencies for the evaluation harness only (not needed to run Harmonia).
#
# librosa, numpy, scipy and soundfile are NOT listed here: they come from the
# main application requirements, and the analyzer under test already pins them.
# Install into the same environment as the app:
#     pip install -r eval/requirements.txt

mirdata>=0.3.8      # dataset download + loaders (GiantSteps+ key, GTZAN tempo)
mir_eval>=0.7       # cross-checks our MIREX key scoring in the test suite
requests>=2.28      # direct audio fetch from the GTZAN mirror
